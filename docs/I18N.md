# Internationalization (i18n)

## Overview

Localization support using react-i18next.

## Adding Translations

1. Add translation files in `src/locales/<lang>/`
2. Update the language configuration
3. Use `useTranslation()` hook in components

## Supported Languages

- English (default)
- More coming soon
