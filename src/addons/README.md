# Addons Runtime

Core addon system runtime and host bridge.

## Components

- `addons-core.ts` - Core addon management
- `addons-loader.ts` - Dynamic addon loading
- `addons-dev-mode.ts` - Development mode support

## Architecture

Addons run in isolated contexts with controlled API access.
